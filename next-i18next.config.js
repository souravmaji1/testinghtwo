module.exports = {
    i18n: {
      // all the locales supported in the application
      locales: ['en', 'it', 'es', 'fr', 'rs', 'ar', 'de', 'pr', 'zh'], 
      // the default locale to be used when visiting
      // a non-localized route (e.g. `/about`)   
      defaultLocale: 'en'
    },
  }